{
  "name": "explorer-crawlers",
  "version": "1.0.0",
  "private": true,
  "description": "Deterministic football intelligence engine and dataset builder",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=apps/web-dashboard",
    "dev:backend": "npm run start:dev --workspace=apps/crawler-engine",
    "dev:all": "npx concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}